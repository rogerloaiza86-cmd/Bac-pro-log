# Secrets and environment
.env
.env.local
.env.*.local
supabase-config.js.local

# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build outputs
.vercel/
dist/
build/

# OS files
.git
.DS_Store
Thumbs.db
*.log
.vscode/
.antigravity/

# Local Claude Code settings (contains secrets, never commit)
.claude/settings.local.json
.mcp.json

# ZCode session artifacts (plans, internal state)
.zcode/

# FANALIO charte (document externe, ne pas committer)
Charte*.pdf
fanalio_page_*.png
