{
  "name": "bac-pro-logistique",
  "version": "5.0.0",
  "description": "Plateforme collaborative de mutualisation des scénarios pédagogiques pour le Bac Pro Métiers de la Logistique — Référentiel 2025.",
  "private": true,
  "scripts": {
    "build": "echo \"Build skipped — supabase-config.js is committed, no build step needed.\"",
    "build:config": "node scripts/build.js",
    "migrate": "node scripts/migrate-scenarios.js",
    "serve": "npx serve ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "serve": "^14.2.3",
    "supabase": "^2.110.0"
  }
}
